# Environment — never commit secrets
.env

# Node.js dependencies
node_modules/

# Machine-managed scheduler state (created automatically on first run)
task-state.json
agentscheduler.db
agentscheduler.db-*
task-loop.lock
/task-loop
logs/*.log
launchd/*.plist

# Generated daily session export files (optional: track them if you want history in git)
# memory/history/*.md

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/settings.json
